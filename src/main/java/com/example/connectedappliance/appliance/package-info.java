/**
 * Appliance registration, common metadata, lifecycle, and collection-configuration ownership.
 *
 * <p>This module may depend on Shared types and declare its own outbound ports. Other modules must
 * use future public application or query ports and must never access this module's repository or
 * persistence entity directly.
 */
package com.example.connectedappliance.appliance;
