/**
 * Vendor adapter registration, exact-key lookup, scenario execution, and consumer-port
 * implementations.
 *
 * <p>This package may implement public Appliance and Metrics outbound ports but must not access
 * feature infrastructure, persistence, controllers, or reporting.
 */
package com.example.connectedappliance.vendor.application;
